---
import GuideCard from "@/components/cards/guide-card.astro";
import DocsPageHeader from "@/components/page-header.astro";
import DocsLayout from "@/layouts/docs-layout.astro";
import { getGuides } from "@/lib/fetchers";

const guides = await getGuides();
---

<DocsLayout title="Guides" description="Guides">
  <section class="container max-w-6xl py-10 mb-48 h-full">
    <DocsPageHeader
      heading="Guides"
      text="This section is currently under development"
    />

    {
      guides?.length ? (
        <div class="grid gap-4 md:grid-cols-2 md:gap-6">
          {guides.map((guide) => (
            <GuideCard {...guide} />
          ))}
        </div>
      ) : (
        <p>Soon </p>
      )
    }
  </section>
</DocsLayout>
